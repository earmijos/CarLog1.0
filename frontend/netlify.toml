# Netlify configuration for Flutter web app
# Since Netlify can't build Flutter, we use the pre-built web folder

[build]
  publish = "build/web"
  command = "echo 'Using pre-built Flutter web app'"

# Handle client-side routing for Flutter
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

